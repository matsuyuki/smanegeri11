{% extends 'SMASMABundle:Admin:template.html.twig' %}

{% block body -%}
    <h1>Data Nilai Siswa</h1>

    <table class="table">
        <thead>
            <tr>
                
                <th align="center">NIS</th>
                <th align="center">Nama</th>
                <th align="center">Mata Pelajarang</th>
                <th align="center">Tugas</th>
                <th align="center">UTS</th>
                <th align="center">UAS</th>
                <th align="center">Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for entity in entities%}
                <tr>
                    <td align="center"><a href="{{ path('nilai_show', { 'id': entity.id }) }}">{{ entity.nis }}</a></td>
                    <td align="center">{{ entity.nama}}</td>
                    <td align="center">{{ entity.matapelajaran }}</td>
                    <td align="center">{{ entity.Tugas }}</td>
                    <td align="center">{{ entity.UTS }}</td>
                    <td align="center">{{ entity.UAS }}</td>
                    <td align="center">
                <ul>
                    <li>
                        <a href="{{ path('siswa_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('siswa_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
        <a href="{{ path('nilai_new') }}"><button class="button">Tambah Data</button></a>
      

            {% endblock %}
